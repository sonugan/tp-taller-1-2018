#ifndef PITCH_H
#define PITCH_H

#include "goal-zone.h"

class Pitch
{
    public:
        Pitch();
        virtual ~Pitch();
        //bool IsGoalOnEastGoal(Ball* ball);
        //bool IsGoalOnWestGoal(Ball* ball);
    protected:
        //GoalZone* east_goal;
        //GoalZone* west_goal;
    private:
};

#endif // PITCH_H
